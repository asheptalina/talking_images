import SwiftUI

struct VoiceView: View {
    var body: some View {
        Text("Voice")
    }
}

struct VoiceView_Previews: PreviewProvider {
    static var previews: some View {
        VoiceView()
    }
}
